name: phober-backend

# ──────────────────────────────────────────────────────────────────────────────
# Reusable building blocks
# ──────────────────────────────────────────────────────────────────────────────

x-php-image: &php-image abdukhaligov/phober-php-apache:2.0.0

x-php-base: &php-base
    image: *php-image
    restart: unless-stopped
    networks: [private]
    entrypoint: sh -c "apache2 -D FOREGROUND"

x-healthcheck: &healthcheck
    test: ["CMD", "curl", "-fsS", "http://localhost/up"]
    interval: 10m
    retries: 5
    start_period: 10m
    timeout: 1m

x-shared-laravel-volume: &shared-laravel-volume
    ./shared/laravel-common:/var/www/html/shared:ro

# ──────────────────────────────────────────────────────────────────────────────
# Services
# ──────────────────────────────────────────────────────────────────────────────

services:
    nginx-proxy-manager:
        image: jc21/nginx-proxy-manager:latest
        container_name: nginx-proxy-manager
        restart: unless-stopped
        ports:
            - "80:80"
            - "81:81"
            - "443:443"
        volumes:
            - ./docker/npm/data:/data
            - ./docker/npm/letsencrypt:/etc/letsencrypt
        networks: [public, private]

    api-gateway:
        image: nginx:latest
        container_name: api-gateway
        restart: unless-stopped
        volumes:
            - ./docker/api-gateway/certs/nginx.crt:/etc/nginx/ssl/nginx.crt:ro
            - ./docker/api-gateway/certs/nginx.key:/etc/nginx/ssl/nginx.key:ro
            - ./docker/api-gateway/certs/dhparam.pem:/etc/nginx/ssl/dhparam.pem:ro
            - ./docker/api-gateway/nginx.conf:/etc/nginx/nginx.conf
            - ./docker/api-gateway/public:/etc/nginx/public:ro
        networks: [public, private]
        depends_on:
            - notification-server
            - auth-server
            - device-service
            - crm-service
            - staff-service

    adminpanel:
        <<: *php-base
        container_name: adminpanel
        networks: [private, public]
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/s3.env
            - docker/env/reverb.env
            - docker/env/db-admin-default.env
            - docker/env/db-auth-admin.env
            - docker/env/db-device-admin.env
            - docker/env/db-crm-admin.env
            - docker/env/db-staff-admin.env
            - docker/env/db-configs-admin.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_ADMINPANEL}
            AWS_URL: "${API_GATEWAY_HOST:-http://api.phober.test}/s3"
            DB_QUEUE: adminpanel
        volumes:
            - *shared-laravel-volume
            - ./services/adminpanel:/var/www/html

    auth-server:
        <<: *php-base
        container_name: auth-server
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/reverb.env
            - docker/env/db-auth-admin.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_AUTH_SERVER}
            DB_CONNECTION: sqlite
        volumes:
            - *shared-laravel-volume
            - ./services/auth-server:/var/www/html

    config-server:
        <<: *php-base
        container_name: config-server
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/db-configs-admin.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_CONFIG_SERVER}
            USE_CACHE: "false"
        volumes:
            - *shared-laravel-volume
            - ./services/config-server:/var/www/html

    notification-server:
        <<: *php-base
        container_name: notification-server
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_NOTIFICATION_SERVER}
            DISCORD_TOKEN: ${DISCORD_TOKEN:-}
            DISCORD_SUPPORT_CHANNEL: ${DISCORD_SUPPORT_CHANNEL:-}
            DISCORD_OTP_CHANNEL: ${DISCORD_OTP_CHANNEL:-}
            TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
            TELEGRAM_OTP_CHANNEL: ${TELEGRAM_OTP_CHANNEL:-}
            TELEGRAM_SUPPORT_CHANNEL: ${TELEGRAM_SUPPORT_CHANNEL:-}
            DB_QUEUE: notification
        volumes:
            - *shared-laravel-volume
            - ./services/notification-server:/var/www/html

    device-service:
        <<: *php-base
        container_name: device-service
        healthcheck: *healthcheck
        deploy:
            replicas: 1
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/s3.env
            - docker/env/reverb.env
            - docker/env/db-device-default.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_DEVICE_SERVICE}
            APP_URL: /hardware
            AWS_URL: /s3/media
            DB_QUEUE: device
        volumes:
            - *shared-laravel-volume
            - ./services/device-service:/var/www/html

    crm-service:
        <<: *php-base
        container_name: crm-service
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/db-crm-default.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_CRM_SERVICE}
        volumes:
            - *shared-laravel-volume
            - ./services/crm-service:/var/www/html

    staff-service:
        <<: *php-base
        container_name: staff-service
        healthcheck: *healthcheck
        env_file:
            - docker/env/apache.env
            - docker/env/laravel.env
            - docker/env/redis.env
            - docker/env/mail.env
            - docker/env/reverb.env
            - docker/env/db-staff-default.env
            - docker/env/db-shared-admin.env
        environment:
            APP_KEY: ${APP_KEY_STAFF_SERVICE}
            DB_QUEUE: staff
        volumes:
            - *shared-laravel-volume
            - ./services/staff-service:/var/www/html

networks:
    private:
        external: true
        name: phober_private
    public:
        external: true
        name: phober_public
